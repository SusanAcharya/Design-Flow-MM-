/* The app's own mark, kept beside Mitra's poses so brand art has one home.
   The coin is drawn on black, so every use crops it to a circle. */
const base = import.meta.env.BASE_URL;

export const logo = `${base}logo.jpg`;
